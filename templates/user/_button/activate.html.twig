<input
    name="_token"
    type="hidden"
    value="{{csrf_token(csrfName)}}"
>
<button
    class="btn btn-success icon-accept"
    id="{{btnId|default('')}}"
    type="submit"
>
    {{'Activate'|trans({}, 'User')}}
</button>
