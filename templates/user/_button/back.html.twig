<a
    class="btn btn-outline-secondary icon-cancel"
    href="{{ path(path|default('users_list')) }}"
    role="button"
    title="{{'Go back to the list'|trans({}, 'User')}}"
>
    {{text|default('Cancel')|trans({}, 'User')}}
</a>
