<input
    name="_token"
    type="hidden"
    value="{{csrf_token(csrfName)}}"
>
<button
    class="btn btn-outline-danger icon-reject"
    id="{{btnId|default('')}}"
    type="submit"
>
    {{'Deactivate'|trans({}, 'User')}}
</button>
