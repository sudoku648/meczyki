<input
    name="_token"
    type="hidden"
    value="{{ csrf_token(csrfName) }}"
>
<button
    class="btn btn-success btn-sm icon-accept"
    type="submit"
>
    {{'Activate'|trans({}, 'User')}}
</button>
