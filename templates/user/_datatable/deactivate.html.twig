<input
    name="_token"
    type="hidden"
    value="{{ csrf_token(csrfName) }}"
>
<button
    class="btn btn-outline-danger btn-sm icon-reject"
    type="submit"
>
    {{'Deactivate'|trans({}, 'User')}}
</button>
