{% extends 'main.html.twig' %}

{% block title %}{{'Users'|trans({}, 'User')}}{% endblock %}

{% block mainContent %}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\UserVoter::CREATE')) %}
        {{ include('user/_button/add.html.twig') }}
    {% endif %}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\UserVoter::ACTIVATE_BATCH')) %}
        {{ include('user/_activate_batch_form.html.twig') }}
    {% endif %}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\UserVoter::DEACTIVATE_BATCH')) %}
        {{ include('user/_deactivate_batch_form.html.twig') }}
    {% endif %}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\UserVoter::DELETE_BATCH')) %}
        {{ include('user/_delete_batch_form.html.twig') }}
    {% endif %}

    <div class="mb-3">
        <table class="dataTable table table-hover table-responsive" id="users-list">
            <thead>
                <tr>
                    <th>{{'Ord. num.'|trans({}, 'DataTable')}}</th>
                    <th><input type="checkbox" id="checkbox_users_all" class="form-check-input"></th>
                    <th>{{'Username'|trans({}, 'User')}}</th>
                    <th>{{'Actions'|trans({}, 'DataTable')}}</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
            <tfoot>
                <tr>
                    <th>{{'Ord. num.'|trans({}, 'DataTable')}}</th>
                    <th></th>
                    <th class="s">{{'Username'|trans({}, 'User')}}</th>
                    <th>{{'Actions'|trans({}, 'DataTable')}}</th>
                </tr>
            </tfoot>
        </table>
    </div>
{% endblock %}
