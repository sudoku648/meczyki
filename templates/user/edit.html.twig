{% extends 'main.html.twig' %}

{% block title %}{{'Edit user'|trans({}, 'User')}}{% endblock %}

{% block mainContent %}
    <fieldset class="border p-2 mb-3">
        <legend class="w-auto">{{'Username'|trans({}, 'User')}}</legend>
        {{ include('user/_form_basic.html.twig', {'form': form_basic}) }}
    </fieldset>
    <fieldset class="border p-2">
        <legend class="w-auto">{{'Password'|trans({}, 'User')}}</legend>
        {{ include('user/_form_password.html.twig', {'form': form_password}) }}
    </fieldset>
{% endblock %}
