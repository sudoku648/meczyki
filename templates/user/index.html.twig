{% extends 'main.html.twig' %}

{% block title %}Użytkownicy{% endblock %}

{% block main_content %}
    {% if is_granted(constant('\\App\\Security\\Voter\\UserVoter::CREATE')) %}
        {{ include('buttons/add.html.twig', {path: 'user_create', parameters: {}, text: 'Dodaj nowego'}) }}
    {% endif %}
    {% if is_granted(constant('\\App\\Security\\Voter\\UserVoter::DELETE_BATCH')) %}
        {{ include('user/_delete_batch_form.html.twig') }}
    {% endif %}

    <div class="mb-3">
        <table class="dataTable table table-hover table-responsive" id="users-list">
            <thead>
                <tr>
                    <th>L.p.</th>
                    <th></th>
                    <th class="s">Nazwa użytkownika</th>
                    <th>Akcje</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
{% endblock %}
