{{ form_start(form) }}
    {{ form_row(form.username, {'label': 'Username'}) }}

    {{ form_row(form.save, {
        'label': 'Save',
        'attr': {'class': 'btn-success icon-save loadable-save'},
    }) }}
    {{ form_row(form.saveAndContinue, {
        'label': 'Save and continue editing',
        'attr': {'class': 'btn-success icon-save loadable-save'},
    }) }}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\UserVoter::LIST')) %}
        {{ include('user/_button/back.html.twig') }}
    {% endif %}
{{ form_end(form) }}
