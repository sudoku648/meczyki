{% extends 'main.html.twig' %}

{% block title %}Osoby{% endblock %}

{% block main_content %}
    {{ include('person/_types_buttons.html.twig') }}

    {% if is_granted(constant('\\App\\Security\\Voter\\PersonVoter::CREATE')) %}
        {{ include('buttons/add.html.twig', {path: 'person_create', parameters: {}, text: 'Dodaj nową'}) }}
    {% endif %}

    <div class="mb-3">
        <table class="dataTable table table-hover table-responsive" id="people-list">
            <thead>
                <tr>
                    <th>L.p.</th>
                    <th class="s">Nazwisko i imię</th>
                    <th>Funkcje</th>
                    <th>Akcje</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
{% endblock %}
