{% extends 'main.html.twig' %}

{% block title %}Delegaci{% endblock %}

{% block main_content %}
    {{ include('person/_types_buttons.html.twig') }}

    {% if is_granted(constant('\\App\\Security\\Voter\\PersonVoter::CREATE')) %}
        {{ include('buttons/add.html.twig', {path: 'delegate_create', parameters: {}, text: 'Dodaj nowego'}) }}
    {% endif %}
    {% if is_granted(constant('\\App\\Security\\Voter\\PersonVoter::DELETE_BATCH')) %}
        {{ include('person/_delete_batch_form.html.twig') }}
    {% endif %}

    <div class="mb-3">
        <table class="dataTable table table-hover table-responsive" id="delegates-list">
            <thead>
                <tr>
                    <th>L.p.</th>
                    <th><input type="checkbox" id="checkbox_people_all" class="form-check-input"></th>
                    <th class="s">Nazwisko i imię</th>
                    <th>Akcje</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
{% endblock %}
