{{ include('form/delete_confirmation.html.twig') }}
<form
    action="{{ path('person_delete_batch') }}"
    class="d-md-inline-block align-middle mb-3"
    id="people-delete-batch"
    method="post"
    data-confirmation="true"
    data-bs-toggle="modal"
    data-bs-target="#staticBackdrop"
>
    {{ include('person/_button/delete.html.twig', {
        csrfName: 'person_delete_batch',
        btnId: 'people-delete-batch-btn',
    }) }}
</form>
