{{ include('form/delete_confirmation.html.twig') }}
<form
    class="d-none d-md-inline-block form-inline ms-auto my-2 my-md-0"
    action="{{ path('person_delete', {'person_id': person.id}) }}"
    method="post"
    data-confirmation="true"
    data-bs-toggle="modal"
    data-bs-target="#staticBackdrop"
>
    {{ include('person/_datatable/delete.html.twig', {csrfName: 'person_delete'}) }}
</form>
