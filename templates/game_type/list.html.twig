{% extends 'main.html.twig' %}

{% block title %}{{'Game types'|trans({}, 'GameType')}}{% endblock %}

{% block mainContent %}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\GameTypeVoter::CREATE')) %}
        {{ include('game_type/_button/add.html.twig', {
            path: 'game_type_create',
            text: 'Add new',
        }) }}
    {% endif %}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\GameTypeVoter::DELETE_BATCH')) %}
        {{ include('game_type/_delete_batch_form.html.twig') }}
    {% endif %}

    <div class="mb-3">
        <table class="dataTable table table-hover table-responsive" id="game-types-list">
            <thead>
                <tr>
                    <th>{{'Ord. num.'|trans({}, 'DataTable')}}</th>
                    <th><input type="checkbox" id="checkbox_gameTypes_all" class="form-check-input"></th>
                    <th>{{'Name'|trans({}, 'GameType')}}</th>
                    <th>{{'Actions'|trans({}, 'DataTable')}}</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
            <tfoot>
                <tr>
                    <th>{{'Ord. num.'|trans({}, 'DataTable')}}</th>
                    <th></th>
                    <th>{{'Name'|trans({}, 'GameType')}}</th>
                    <th>{{'Actions'|trans({}, 'DataTable')}}</th>
                </tr>
            </tfoot>
        </table>
    </div>
{% endblock %}
