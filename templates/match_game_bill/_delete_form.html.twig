{{ include('form/delete_confirmation.html.twig') }}
<form
    action="{{ path('match_game_bill_delete', {'match_game_id': matchGameBill.matchGame.id, 'match_game_bill_id':  matchGameBill.id}) }}"
    class="d-none d-md-inline-block form-inline ms-auto my-2 my-md-0"
    method="post"
    data-confirmation="true"
    data-bs-toggle="modal"
    data-bs-target="#staticBackdrop"
>
    {{ include('match_game_bill/_button/delete.html.twig', {csrfName: 'match_game_bill_delete'}) }}
</form>
