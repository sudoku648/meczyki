{{ form_start(form) }}
    {{ form_row(form.name, {'label': 'Full name'}) }}
    {{ form_row(form.shortName, {'label': 'Short name'}) }}

    {{ form_row(form.save, {
        'label': 'Save',
        'attr': {'class': 'btn-success icon-save loadable-save'},
    }) }}
    {{ form_row(form.saveAndContinue, {
        'label': (buttonSaveAndContinueLabel|default('Save and add new')),
        'attr': {'class': 'btn-success icon-save loadable-save'},
    }) }}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\TeamVoter::LIST')) %}
        {{ include('team/_button/back.html.twig') }}
    {% endif %}
{{ form_end(form) }}
