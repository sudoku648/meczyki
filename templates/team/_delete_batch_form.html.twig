{{ include('form/delete_confirmation.html.twig') }}
<form
    action="{{ path('team_delete_batch') }}"
    class="d-md-inline-block align-middle mb-3"
    id="teams-delete-batch"
    method="post"
    data-confirmation="true"
    data-bs-toggle="modal"
    data-bs-target="#staticBackdrop"
>
    {{ include('team/_button/delete.html.twig', {
        csrfName: 'team_delete_batch',
        btnId: 'teams-delete-batch-btn',
    }) }}
</form>
