{% extends 'main.html.twig' %}

{% block title %}{{'Clubs'|trans({}, 'Club')}}{% endblock %}

{% block mainContent %}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\ClubVoter::CREATE')) %}
        {{ include('club/_button/add.html.twig', {
            path: 'club_create',
            text: 'Add new',
        }) }}
    {% endif %}
    {% if is_granted(constant('\\Sudoku648\\Meczyki\\Security\\Infrastructure\\Voter\\ClubVoter::DELETE_BATCH')) %}
        {{ include('club/_delete_batch_form.html.twig') }}
    {% endif %}

    <div class="mb-3">
        <table class="dataTable table table-hover table-responsive" id="clubs-list">
            <thead>
                <tr>
                    <th>{{'Ord. num.'|trans({}, 'DataTable')}}</th>
                    <th><input type="checkbox" id="checkbox_clubs_all" class="form-check-input"></th>
                    <th>{{'Name'|trans({}, 'Club')}}</th>
                    <th>{{'Actions'|trans({}, 'DataTable')}}</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
            <tfoot>
                <tr>
                    <th>{{'Ord. num.'|trans({}, 'DataTable')}}</th>
                    <th></th>
                    <th class="s">{{'Name'|trans({}, 'Club')}}</th>
                    <th>{{'Actions'|trans({}, 'DataTable')}}</th>
                </tr>
            </tfoot>
        </table>
    </div>
{% endblock %}
