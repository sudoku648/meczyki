{{ include('form/delete_confirmation.html.twig') }}
<form
    action="{{ path('club_delete_batch') }}"
    class="d-md-inline-block align-middle mb-3"
    id="clubs-delete-batch"
    method="post"
    data-confirmation="true"
    data-bs-toggle="modal"
    data-bs-target="#staticBackdrop"
>
    {{ include('club/_button/delete.html.twig', {
        csrfName: 'club_delete_batch',
        btnId: 'clubs-delete-batch-btn',
    }) }}
</form>
