{% if btn_size is defined %}
    {% if btn_size == 'table' %}
        {% set btnClass = 'btn-sm' %}
    {% else %}
        {% set btnClass = btn_size %}
    {% endif %}
{% else %}
    {% set btnClass = '' %}
{% endif %}

{% block button %}
{% endblock %}
